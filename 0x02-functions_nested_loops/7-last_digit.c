#include "main.h"
#include <stdio.h>
/**
 * print_last_digit -  prints the last 
 * digit of a number
 * @n: character used.
 * Return: Always 0.
 */
int print_last_digit(int j)
{
	int j;

	if (j % 10)
	{
		return (0);
	}
}

#include <stdio.h>
/**
 * main - entry point
 * Return: Always 0.
 */
int main(void)
{
	int  n;
	char alpha;

	if (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	for (alpha = 'a'; alpha < 'h'; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}

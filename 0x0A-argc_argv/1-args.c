#include <stdio.h>
/**
 * main - entry point
 * argc - count arguments
 * argv - arrays to pointers
 * Return: Always 0.
 */
int main(int argc, __attribute__((unused))  char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
